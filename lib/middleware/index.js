const fakerMatcher = require('./fakerMatcher.js');
const requireProps = require('./requireProps.js');
const fakerDate = require('./fakerDate');

module.exports = {
  fakerMatcher,
  requireProps,
  fakerDate
};